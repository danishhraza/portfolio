/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    distDir: 'build',
    output: 'standalone'
    // Keep any other existing configurations you already have here
  };
  
  export default nextConfig;